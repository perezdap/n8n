# ==============================================================================
# n8n Deployment Environment Variables
# ==============================================================================
# Copy this file to .env and update the values for your environment.
# ==============================================================================

# 1. PostgreSQL Configuration
POSTGRES_USER=n8n_user
POSTGRES_PASSWORD=change_me_securely
POSTGRES_DB=n8n_db

# 2. Domain & SSL Configuration
# Your public domain name (e.g., n8n.example.com)
DOMAIN_NAME=n8n.localhost
# Email for Let's Encrypt expiration notifications
SSL_EMAIL=admin@example.com

# 3. Security (Optional but recommended)
# n8n encryption key (generate a random string)
# N8N_ENCRYPTION_KEY=your_random_key
